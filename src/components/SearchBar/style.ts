import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  searchBar: {
    width: 320,
    height: 60,
    backgroundColor: "#023970",
    display: "flex",
    flexDirection: "row",
    alignItems: "center",
    gap: 125,
    paddingLeft: 25,
    borderRadius: 25,
    alignSelf: "center"
   }, 
  search: {
    fontSize: 18,
  }
});